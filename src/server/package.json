{
  "name": "server",
  "version": "1.0.0",
  "description": "mymony server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-server": "tsc",
    "watch-server": "tsc -w",
    "server": "node ./dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^7.0.0-rc.1",
    "mongodb": "^3.5.6"
  },
  "devDependencies": {
    "@types/elasticsearch": "^5.0.36",
    "@types/mongodb": "^3.5.10"
  }
}
